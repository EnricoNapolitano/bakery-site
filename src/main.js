import { createApp } from 'vue'
import "@fontsource/playfair-display" //font for titles -> to import in style: { font-family: "Playfair Display", serif; }
import "@fontsource/work-sans" //font for titles -> to import in style: { font-family: "Work Sans", sans-serif; }
import App from './App.vue'

createApp(App).mount('#app')
